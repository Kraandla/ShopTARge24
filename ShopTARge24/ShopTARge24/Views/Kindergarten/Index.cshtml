@model IEnumerable<ShopTARge24.Models.Kindergarten.KindergartenIndexViewModel>

@{
    int rowNumber = 1;
}

<div>
    <a id="KindergartensCreateTest" asp-action="create" asp-controller="Kindergarten" type="submit" class="btn btn-primary">Create</a>
</div>

<div class="h1">
    Kindergartens
</div>
<form method="get">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Nr</th>
                <th scope="col">Kindergarten Name</th>
                <th scope="col">Group Name</th>
                <th scope="col">Child Count</th>
                <th scope="col">Created at</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Kindergarten in Model)
            {
                <tr>
                    <td>@rowNumber</td>
                    <td>@Kindergarten.KindergartenName</td>
                    <td>@Kindergarten.GroupName</td>
                    <td>@Kindergarten.ChildCount</td>
                    <td>@Kindergarten.CreatedAt</td>
                    <td>
                        <a id="KindergartensDetailsTest" asp-action="details" asp-controller="Kindergarten" asp-route-id="@Kindergarten.Id" class="btn btn-primary">Details</a>
                        <a id="KindergartensUpdateTest" asp-action="update" asp-controller="Kindergarten" asp-route-id="@Kindergarten.Id" class="btn btn-primary">Update</a>
                        <a id="KindergartensDeleteTest" asp-action="delete" asp-controller="Kindergarten" asp-route-id="@Kindergarten.Id" class="btn btn-outline-danger">Delete</a>
                    </td>
                </tr>
                rowNumber += 1;
            }
        </tbody>
    </table>
</form>

